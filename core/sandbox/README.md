# core/sandbox — Isolation d'exécution

Conteneur isolé + branche Git par tâche ; permissions scopées ; plafonds.

> **Placeholder** — l'implémentation POC vit dans le paquet Python : [`maestro/sandbox/`](../../maestro/sandbox/)
> (isolation au niveau système de fichiers pour l'instant ; conteneur par tâche prévu ensuite).
